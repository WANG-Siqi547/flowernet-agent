fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7

# Render free instances run UniEval with UNIEVAL_LIGHTWEIGHT_MODE=true.
# Keep transformer dependencies out of the default image so deploys do not fail
# while downloading/building torch. For a dedicated semantic-model deployment,
# install these optional packages and set UNIEVAL_LIGHTWEIGHT_MODE=false:
# transformers>=4.55.0,<5.0.0
# torch>=2.8.0
# sentencepiece>=0.2.0
# safetensors>=0.4.3
# huggingface-hub>=0.34.0
